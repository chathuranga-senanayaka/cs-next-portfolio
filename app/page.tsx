import { Button } from "@/components/ui/button";
import React from "react";

const Home = () => {
  return (
    <div>
      <Button variant={"dark"} size={"primary"}>
        Get started now{" "}
      </Button>
    </div>
  );
};

export default Home;
